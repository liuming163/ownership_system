# 权属管理系统 - 用户使用指南

## 一、系统简介

权属管理系统用于管理代理主体、被代理人及作品的权属信息，支持文件上传、批量打包下载、历史版本追溯等功能。

**适用场景：** 版权代理公司管理作品权属证明、授权委托书等法律文件。

---

## 二、登录系统

1. 打开系统访问地址（默认 `http://localhost:5173`）
2. 进入登录页面，输入用户名和密码
3. 点击「登录」按钮
4. 登录成功后自动跳转到代理主体管理页面

---

## 三、代理主体管理

**路径：** 系统首页 `/companies`

### 3.1 新增代理主体

1. 点击右上角「新增代理主体」按钮
2. 填写信息：
   - **公司名称**（必填）：输入代理主体的公司全称
   - **营业执照**（必填）：上传营业执照文件（支持 JPG/PNG/PDF，超过5MB自动压缩）
   - **营业期限**：勾选「长期」或选择截止日期
3. 点击「确定」提交

**注意事项：**
- 公司名称一旦创建不可修改
- 文件会自动命名为 `营业执照_{公司名}.{ext}`

### 3.2 编辑代理主体

1. 点击操作列的「编辑」按钮
2. 可修改：营业执照文件、营业期限
3. **不可修改**：公司名称（显示为禁用状态）

### 3.3 删除代理主体

1. 点击操作列的「删除」按钮
2. 确认删除提示
3. 删除后关联的被代理人和作品数据不会被删除（保留历史记录）

---

## 四、被代理人管理

**路径：** `/agents`

### 4.1 新增被代理人

1. 点击右上角「新增被代理人」按钮
2. 填写信息：
   - **代理主体**（必填）：从下拉列表选择
   - **被代理人名称**（必填）：输入被代理人公司或个人名称
   - **被代理人营业执照**（必填）：上传营业执照文件
   - **营业期限**：勾选「长期」或选择截止日期
   - **授权委托书**（必填）：上传授权委托书文件
   - **授权截止日期**（必填）：选择授权有效期截止日期
3. 点击「确定」提交

**文件命名规则：**
- 被代理人营业执照：`营业执照_{被代理人名}.{ext}`
- 授权委托书：`授权委托书_{被代理人名}_截止{YYYYMMDD}.{ext}`

### 4.2 筛选被代理人

1. 使用页面顶部的「按代理主体筛选」下拉框
2. 选择代理主体后，表格自动过滤显示该主体下的被代理人

### 4.3 更新授权委托书

**场景：** 授权委托书即将到期，需要续签

1. 点击操作列的「更新授权」按钮
2. 弹窗中可修改：
   - 授权委托书文件（可选，不选则只更新截止日期）
   - 授权截止日期（可选，不选则只更新文件）
3. 点击「确定」提交

**重要特性：**
- 旧的授权委托书文件会保留在磁盘，不会被删除
- 历史授权记录会保存在「历史」中，可追溯

### 4.4 查看授权历史

1. 点击操作列的「历史」按钮
2. 弹窗显示该被代理人的所有授权委托书历史记录
3. 包含：授权文件、授权截止日期、更新时间、更新人

### 4.5 删除被代理人

1. 点击操作列的「删除」按钮
2. 确认删除提示
3. 删除后关联的作品数据不会被删除

---

## 五、作品管理

**路径：** `/works`

### 5.1 添加作品

1. 点击右上角「添加作品」按钮
2. 填写信息：
   - **代理主体**（必填）：从下拉列表选择
   - **被代理人**（必填）：选择代理主体后，此下拉框自动加载该主体下的被代理人
   - **作品名称**（必填）：输入作品完整名称
   - **别名**（可选）：多个别名用下划线 `_` 拼接，例如 `别名1_别名2_别名3`
   - **权属证明**（必填）：上传权属证明文件（支持 JPG/PNG/PDF）
   - **其他证明**（可选）：最多上传2个其他证明文件
3. 点击「确定」提交

**文件命名规则：**
- 权属证明：`权属证明_{作品名}_{随机8位}.{ext}`
- 其他证明：`其他证明_{作品名}_{序号}_{随机8位}.{ext}`

### 5.2 更新作品

**场景：** 需要修改别名、替换权属证明或其他证明文件

1. 点击操作列的「更新」按钮
2. 弹窗显示作品完整信息
3. **不可修改项**（显示为禁用状态）：
   - 代理主体
   - 被代理人
   - 作品名称
4. **可修改项**：
   - 别名：修改或清空别名
   - 权属证明：上传新文件会**替换**原文件（旧文件会被删除）
   - 其他证明：上传新文件会**替换全部**原文件（旧文件会被删除）
5. 改哪个提交哪个，至少修改一项才能提交
6. 点击「确定」提交

**更新记录：**
- 表格中会显示「最近更新人」和「最近更新时间」
- 首次创建时，更新人=创建人，更新时间=创建时间

### 5.3 搜索作品

**方式一：关键词搜索**
1. 在页面顶部搜索框输入作品名称或别名
2. 支持多关键词搜索：用下划线 `_` 分隔，例如 `作品1_别名2`
3. 实时过滤显示匹配结果
4. 未匹配的关键词会在表格上方黄色警告框中提示

**方式二：Excel批量导入**
1. 点击「从Excel获取作品名称」按钮
2. 上传Excel文件（支持 .xlsx 或 .xls）
3. 选择「作品名称所在的列」（默认A列）
4. 选择「起始行」（默认第2行，第1行为表头）
5. 点击「确定」，系统自动提取作品名称并过滤表格
6. Excel中未匹配到的作品名称会在表格上方黄色警告框中提示

**方式三：筛选**
1. 使用「代理主体」下拉框筛选
2. 选择代理主体后，「被代理人」下拉框自动加载该主体下的被代理人
3. 选择被代理人后，表格显示该被代理人的所有作品

### 5.4 查看文件

**查看权属证明：**
1. 点击「权属证明」列的「查看」按钮
2. 新窗口打开文件预览或下载

**查看其他证明：**
1. 点击「其他证明」列的「查看(N)」按钮（N为文件数量）
2. 弹窗显示所有其他证明文件列表
3. 点击文件名打开预览或下载

### 5.5 批量打包下载

**场景：** 需要批量下载作品的权属证明文件

1. 勾选需要打包的作品（表格左侧复选框）
2. 点击「打包下载」按钮（按钮会显示已选数量）
3. 设置「单文件打包最大体积(MB)」（默认100MB）
4. 点击「确定」开始打包

**打包规则：**
- 如果所有文件总大小 ≤ 设定值，生成一个压缩包
- 如果超过设定值，自动分包（part1.zip, part2.zip...）
- 压缩包中每个文件的结构：`{代理主体名}_{被代理人名}_{作品名}.{ext}`

### 5.6 删除作品

1. 点击操作列的「删除」按钮
2. 确认删除提示
3. 删除后作品及其关联的权属证明文件会被删除

---

## 六、文件上传说明

### 6.1 支持的文件格式
- **图片**：JPG, JPEG, PNG
- **文档**：PDF

### 6.2 自动压缩机制
当上传文件超过 **5MB** 时，系统自动压缩：
- **图片**：使用 Pillow 压缩，质量75%
- **PDF**：使用 Ghostscript 压缩

**压缩结果提示：**
- 成功：显示「压缩成功，从 X MB 压缩到 Y MB」
- 失败：显示错误信息

### 6.3 文件命名规则汇总

| 文件类型 | 命名格式 | 存储目录 |
|---------|---------|---------|
| 代理主体营业执照 | `营业执照_{公司名}.{ext}` | `uploads/营业执照/` |
| 被代理人营业执照 | `营业执照_{被代理人名}.{ext}` | `uploads/被代理人营业执照/` |
| 授权委托书 | `授权委托书_{被代理人名}_截止{YYYYMMDD}.{ext}` | `uploads/授权委托书/` |
| 权属证明 | `权属证明_{作品名}_{uuid8}.{ext}` | `uploads/权属证明/` |
| 其他证明 | `其他证明_{作品名}_{序号}_{uuid8}.{ext}` | `uploads/权属证明/` |

---

## 七、常见使用场景

### 场景1：新签约一个代理主体
1. 先在「代理主体管理」中添加代理主体（上传营业执照）
2. 再在「被代理人管理」中添加该主体下的被代理人（上传授权委托书）
3. 最后在「作品管理」中添加作品（上传权属证明）

### 场景2：授权委托书即将到期
1. 进入「被代理人管理」页面
2. 找到对应的被代理人，点击「更新授权」
3. 上传新的授权委托书，更新授权截止日期
4. 旧的授权记录保留在「历史」中

### 场景3：批量导出某代理主体的所有作品权属证明
1. 进入「作品管理」页面
2. 使用筛选功能选择代理主体和被代理人
3. 勾选需要导出的作品（或全选）
4. 点击「打包下载」，设置单包大小
5. 下载生成的压缩包

### 场景4：查找某个作品的别名
1. 在「作品管理」页面的搜索框输入作品名称
2. 查看表格中「别名」列
3. 多个别名用下划线 `_` 分隔显示

### 场景5：更新作品的权属证明文件
1. 进入「作品管理」页面
2. 找到对应作品，点击「更新」按钮
3. 上传新的权属证明文件（会替换旧文件）
4. 旧文件会被自动删除，避免占用磁盘空间
5. 表格中显示「最近更新人」和「最近更新时间」

---

## 八、注意事项

1. **必填项：** 所有标注「必填」的字段必须填写，否则无法提交
2. **文件格式：** 只支持 JPG/PNG/PDF 格式
3. **文件大小：** 超过5MB的文件会自动压缩
4. **别名格式：** 多个别名用下划线 `_` 拼接，不要用空格或逗号
5. **删除操作：** 删除代理主体或被代理人不会级联删除作品，但建议先清理作品再删除
6. **更新作品：** 
   - 前3项（代理主体、被代理人、作品名称）不可修改
   - 至少修改一项（别名、权属证明、其他证明）才能提交
   - 上传新文件会替换并删除旧文件
7. **授权历史：** 更新授权委托书时，旧文件不会被删除，保留完整历史记录
8. **批量打包：** 文件数量过多或体积过大时，会自动分包下载

---

## 九、常见问题

**Q1: 上传文件后提示「压缩失败」？**
- **图片压缩失败：** 检查文件是否损坏或格式不正确
- **PDF压缩失败：** 确认系统已安装 Ghostscript（`gs --version`）

**Q2: 搜索不到某个作品？**
- 检查输入的关键词是否正确（区分大小写）
- 尝试用作品名称或别名分别搜索
- 检查是否被筛选条件过滤掉

**Q3: 打包下载失败？**
- 检查选择的作品是否过多或文件体积过大
- 尝试减少选择数量或调整单包体积限制

**Q4: 更新作品时提示「请至少修改一项内容」？**
- 至少修改别名、权属证明、其他证明中的一项
- 不修改任何内容点击确定会触发此提示

**Q5: 表格中「最近更新人」显示为空？**
- 这是旧数据，在下次更新作品时会自动填充

---

## 十、技术支持

如遇到系统问题或需要帮助，请联系系统管理员。

**系统版本：** v1.0  
**最后更新：** 2026-08-24
