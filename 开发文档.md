# 权属管理系统 - 开发文档

## 项目概述

权属管理系统用于管理代理主体、被代理人、作品权属信息的管理平台。

### 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | Flask 3.x + SQLAlchemy (raw SQL) + PyMySQL |
| 前端 | Vue 3 + Vite + Element Plus + Pinia |
| 数据库 | MySQL（ownership_system） |
| 认证 | 对接外部认证服务（auth_client） |

---

## 项目结构

```
ownership_system/
├── server/                          # 后端
│   ├── app/
│   │   ├── auth/                    # 认证模块
│   │   │   ├── auth_client.py       # 外部认证服务客户端
│   │   │   └── decorators.py        # 登录验证装饰器
│   │   ├── routes/                  # 路由层
│   │   │   ├── agent_routes.py      # 被代理人路由
│   │   │   ├── auth_routes.py       # 认证路由
│   │   │   ├── company_routes.py    # 代理主体路由
│   │   │   ├── file_routes.py       # 文件服务路由
│   │   │   └── work_routes.py       # 作品路由
│   │   ├── services/                # 业务逻辑层
│   │   │   ├── agent_service.py     # 被代理人业务逻辑
│   │   │   ├── company_service.py    # 代理主体业务逻辑
│   │   │   ├── file_service.py      # 文件处理服务
│   │   │   └── work_service.py      # 作品业务逻辑
│   │   ├── utils/                   # 工具模块
│   │   │   ├── response.py          # 统一响应格式
│   │   │   └── validators.py        # 数据校验
│   │   ├── extensions.py            # 数据库扩展
│   │   ├── config.py                # 配置
│   │   └── __init__.py             # 应用工厂
│   ├── migrations/
│   │   └── init.sql                 # 数据库建表脚本
│   ├── uploads/                     # 文件存储目录（自动创建）
│   ├── run.py                       # 启动入口
│   ├── requirements.txt             # Python 依赖
│   └── .env                         # 环境配置
├── client/                          # 前端
│   ├── src/
│   │   ├── api/                     # API 请求
│   │   │   ├── agent.js            # 被代理人 API
│   │   │   ├── auth.js             # 认证 API
│   │   │   ├── company.js          # 代理主体 API
│   │   │   ├── request.js          # Axios 封装
│   │   │   └── work.js             # 作品 API
│   │   ├── components/
│   │   │   └── AppLayout.vue       # 布局组件
│   │   ├── stores/
│   │   │   └── user.js             # 用户状态管理
│   │   ├── views/
│   │   │   ├── AgentView.vue       # 被代理人管理页
│   │   │   ├── CompanyView.vue     # 代理主体管理页
│   │   │   ├── LoginView.vue       # 登录页
│   │   │   └── WorkView.vue        # 作品管理页
│   │   ├── router/
│   │   │   └── index.js            # 路由配置
│   │   ├── App.vue
│   │   └── main.js
│   ├── vite.config.js
│   └── package.json
├── README.md
└── 开发文档.md
```

---

## 快速启动

### 1. 环境要求

- Python 3.8+（生产环境为 3.8，本地开发推荐 3.9+）
- Node.js 16+
- MySQL 5.7+ / 8.0+
- Ghostscript（用于 PDF 压缩，可选）
- 浏览器：现代浏览器（Chrome / Edge / Firefox），依赖 Element Plus

### 2. 初始化数据库

```bash
mysql -u root -p < server/migrations/init.sql
```

### 3. 启动后端

```bash
cd server
python3 -m venv .venv        # 独立虚拟环境
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env          # 首次配置，填入数据库密码等
python run.py
```

后端默认运行在 `http://localhost:5002`（开发模式 debug=True）。

### 4. 启动前端

```bash
cd client
npm install
npm run dev
```

前端默认运行在 `http://localhost:5173`，Vite 自动代理 `/api` 到 Flask（5002）。

> ⚠️ **开发模式不要在生产环境跑**：`run.py` 是 Flask 内置开发服务器，单线程、性能差、无监控。生产环境必须用 `gunicorn`（见下文"生产环境部署"）。

---

## 数据库设计

### 表结构

#### companies（代理主体）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| company_name | VARCHAR(255) | 公司名称 |
| license_file | VARCHAR(500) | 营业执照文件名 |
| period_end | DATE | 营业期限截止日期，NULL 表示长期 |
| is_long_term | TINYINT(1) | 1=长期 |
| created_by | VARCHAR(100) | 创建人 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### agents（被代理人）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| company_id | INT | 所属代理主体 |
| agent_name | VARCHAR(255) | 被代理人名称 |
| license_file | VARCHAR(500) | 营业执照文件名 |
| period_end | DATE | 营业期限截止，NULL 表示长期 |
| is_long_term | TINYINT(1) | 1=长期 |
| auth_file | VARCHAR(500) | 当前授权委托书文件名 |
| auth_expires_on | DATE | 授权截止日期 |
| created_by | VARCHAR(100) | 创建人 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### agent_auth_history（授权委托书变更历史）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| agent_id | INT | 被代理人 ID |
| auth_file | VARCHAR(500) | 授权委托书文件名 |
| auth_expires_on | DATE | 此版本的截止日期 |
| replaced_at | DATETIME | 上传时间 |
| uploaded_by | VARCHAR(100) | 上传人 |

#### works（作品）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| company_id | INT | 代理主体 |
| agent_id | INT | 被代理人 |
| work_name | VARCHAR(255) | 作品名称 |
| alias | VARCHAR(255) | 作品别名（可空，NULL 或空串） |
| proof_file | VARCHAR(500) | 权属证明文件名 |
| other_files | JSON | 其他证明文件列表 |
| created_by | VARCHAR(100) | 创建人 |
| created_at | DATETIME | 创建时间 |
| updated_by | VARCHAR(100) | 最近更新人 |
| updated_at | DATETIME | 最近更新时间 |

---

## API 设计

### 认证相关

#### POST /api/login
登录

**请求体：**
```json
{
  "username": "用户名",
  "password": "密码"
}
```

**响应：**
```json
{
  "success": true,
  "data": {
    "username": "用户名",
    "uid": 123456
  }
}
```

#### POST /api/logout
登出

#### GET /api/user/info
获取当前用户信息

**响应：**
```json
{
  "success": true,
  "data": {
    "username": "用户名",
    "uid": 123456
  }
}
```

### 代理主体

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/companies | 获取代理主体列表 |
| GET | /api/companies/{id} | 获取单个代理主体 |
| POST | /api/companies | 新增代理主体 |
| PUT | /api/companies/{id} | 更新代理主体 |
| DELETE | /api/companies/{id} | 删除代理主体 |

### 被代理人

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/agents | 获取被代理人列表 |
| GET | /api/agents/{id} | 获取单个被代理人 |
| GET | /api/agents/expiring-auth | 获取授权到期提醒 |
| GET | /api/agents/{id}/auth/history | 获取授权历史 |
| POST | /api/agents | 新增被代理人 |
| PUT | /api/agents/{id}/auth | 更新授权委托书 |
| DELETE | /api/agents/{id} | 删除被代理人 |

### 作品

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/works | 获取作品列表 |
| GET | /api/works/{id} | 获取单个作品 |
| POST | /api/works | 新增作品 |
| PUT | /api/works/{id} | 更新作品 |
| DELETE | /api/works/{id} | 删除作品 |
| POST | /api/works/package | 打包下载作品 |

### 文件

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/files/{目录}/{文件名} | 获取文件 |

---

## 业务规则

### 文件上传规则

文件超过 5MB 自动压缩（图片用 Pillow 转为 JPEG，PDF 用 Ghostscript 压缩）。

| 文件类型 | 命名规则 | 存储目录 |
|---------|---------|---------|
| 代理主体营业执照 | `营业执照_{公司名}.{ext}` | `uploads/营业执照/` |
| 被代理人营业执照 | `营业执照_{被代理人名}.{ext}` | `uploads/被代理人营业执照/` |
| 授权委托书 | `授权委托书_{被代理人名}_{代理主体名}_截止{YYYYMMDD}.{ext}` | `uploads/授权委托书/` |
| 权属证明 | `权属证明_{作品名}_{uuid8}.{ext}` | `uploads/权属证明/` |
| 其他证明文件 | `其他证明_{作品名}_{序号}_{uuid8}.{ext}` | `uploads/权属证明/` |

### MIME 嗅探与自动更名

上传时后端会读取文件二进制签名（PNG/JPEG/GIF/BMP/PDF），与声明的扩展名比对：

- **一致**：正常保存，无提示
- **不一致**（如 JPEG 改名为 `.png` 上传）：自动用真实扩展名保存，并通过响应 `warning` 字段告知前端
- 前端在 `CompanyView / AgentView / WorkView` 的成功回调里检测 `res.warning`，用 `ElMessage.warning(res.warning)` 显示（默认 3 秒）

实现位于 `server/app/services/file_service.py` 的 `_sniff_ext()` 和 `_normalize_ext()`。

### 作品打包下载

接口：`POST /api/works/package`

**请求体：**
```json
{
  "work_ids": [1, 2, 3],
  "max_size_mb": 18
}
```

**响应：** 直接返回 ZIP 文件流（含每个作品的权属证明 + 其他证明 + 一份 `作品清单.xlsx`）

- Excel 用 `openpyxl` 生成
- ZIP 临时文件用完即删
- `max_size_mb` 用于限制总大小（防止下载超大包）

### 别名（alias）可空

`works.alias` 在数据库中允许 `NULL`，前端不填写时后端存空串 `''`，不会触发 500。

### 删除限制

### 删除限制

- 删除代理主体时，如有被代理人关联，禁止删除
- 删除被代理人时，如有作品关联，禁止删除
- 删除操作会同时删除关联的数据库记录和本地文件

### 授权到期提醒

- 系统检查所有被代理人的授权截止日期
- 已过期或 30 天内即将过期的被代理人会弹窗提醒
- 提醒标记存储在 sessionStorage 中，key 为 `auth_expiry_notified_{uid}`
- 同一用户本次登录只提醒一次
- 登出后重新登录会重新提醒

### 权限控制

删除按钮仅对特定用户可见（uid = 1717602）。权限判断逻辑集中在 `stores/user.js` 的 `canDelete` getter 中。

```javascript
getters: {
  canDelete() {
    return this.uid === 1717602
  },
},
```

---

## 前端架构

### 状态管理

使用 Pinia 管理用户状态：

```javascript
// stores/user.js
export const useUserStore = defineStore('user', {
  state: () => ({
    username: '',
    uid: null,
  }),
  getters: {
    canDelete() {
      return this.uid === 1717602
    },
  },
  actions: {
    async login(username, password) { ... },
    async fetchUser() { ... },
    async logout() { ... },
  },
})
```

### 路由守卫

路由访问前检查登录状态：

```javascript
// router/index.js
router.beforeEach(async (to) => {
  const userStore = useUserStore()
  if (to.meta.requiresAuth !== false) {
    if (!userStore.username) {
      try {
        await userStore.fetchUser()
      } catch {
        return { name: 'Login', query: { redirect: to.fullPath } }
      }
    }
  }
})
```

### 请求拦截

- 统一在 `request.js` 中处理响应
- 401 响应自动跳转登录页
- 错误信息通过 Element Plus 消息提示

---

## 后端架构

### 三层架构

1. **Routes（路由层）**：接收请求，参数校验，调用 Service
2. **Service（服务层）**：业务逻辑处理，数据库操作
3. **Utils（工具层）**：响应格式、数据校验

### 认证机制

- 使用 `@login_required` 装饰器保护需要登录的接口
- 登录后 session 存储 token、username、uid
- Token 验证通过外部认证服务

---

## 开发指南

### 添加新页面

1. 在 `client/src/views/` 创建 Vue 组件
2. 在 `client/src/router/index.js` 注册路由
3. 在 `client/src/api/` 创建对应的 API 文件

### 添加新接口

1. 在 `server/app/routes/` 添加路由
2. 在 `server/app/services/` 添加业务逻辑
3. 在 `client/src/api/` 添加前端请求

### 修改权限逻辑

权限相关的判断集中在 `stores/user.js` 中：

```javascript
getters: {
  canDelete() {
    return this.uid === 1717602
  },
},
```

如需修改删除权限用户，修改此处即可。

---

## 生产环境部署

### 环境信息

| 项 | 值 |
|---|---|
| 服务器 | `root@8.130.13.169`（Ubuntu 20.04） |
| 项目根目录 | `/opt/ownership_system/` |
| 数据库 | 阿里云公网 RDS MySQL 8.0（数据库名 `ownership_system`） |
| 访问地址 | `http://8.130.13.169:5030` |
| 进程管理 | systemd unit `ownership.service`（gunicorn） |
| 反向代理 | nginx（端口 5030 → gunicorn 127.0.0.1:5020） |
| WSGI 容器 | gunicorn 21.x（2 workers, timeout 60s） |
| Python | 3.8（系统）+ 独立 `.venv` |

### 部署架构

```
Internet (http://8.130.13.169:5030)
   │
   ▼
┌─ nginx (sites-enabled/ownership.conf) ─┐
│  ├─ location /        → 静态文件 (Vue build)
│  └─ location /api/   → proxy_pass 127.0.0.1:5020
└─────────────────────────────────────────┘
   │                              │
   ▼                              ▼
uploads/                       阿里云 RDS 公网
(/opt/ownership_system/       (rm-0jlm4l51sqy3p6o9suo
 server/uploads/)              :55306, db=ownership_system)
   │
   ▼
┌─ gunicorn  127.0.0.1:5020 ─────────────┐
│  systemd: /etc/systemd/system/ownership.service
│  venv:    /opt/ownership_system/server/.venv/
│  logs:    journalctl -u ownership
└─────────────────────────────────────────┘
```

### 一键更新流程（代码提交后）

**本地代码 push 到 GitHub 后**，SSH 登录服务器执行更新脚本即可：

```bash
# 1. SSH 登录服务器
ssh root@8.130.13.169
# 输入密码后进入服务器 shell

# 2. 执行一键更新脚本
bash /opt/ownership_system/deploy/update.sh
```

`deploy/update.sh` 自动完成 6 步：

1. `git pull --rebase --autostash` 拉取最新代码
2. 后端依赖更新（`pip install -r requirements.txt`）
3. 前端依赖 + 构建（`npm install && npm run build`，输出到 `server/static/dist/`）
4. 重启后端（`systemctl restart ownership`）
5. 重载 nginx（`nginx -t && nginx -s reload`）
6. 健康检查（前端首页 + API 登录接口）

### 关键配置文件位置

| 文件 | 路径 |
|---|---|
| 后端 systemd unit | `/etc/systemd/system/ownership.service` |
| nginx 站点配置 | `/etc/nginx/sites-enabled/ownership.conf` |
| 后端 .env（含凭据） | `/opt/ownership_system/server/.env`（chmod 600） |
| 一键更新脚本 | `/opt/ownership_system/deploy/update.sh` |

### 常用运维命令

```bash
# 查看后端运行状态
systemctl status ownership

# 实时跟踪后端日志
journalctl -u ownership -f

# 查看最近 50 行日志
journalctl -u ownership -n 50 --no-pager

# 手动重启后端
systemctl restart ownership

# 手动重载 nginx
nginx -t && nginx -s reload

# 验证端口监听
ss -tlnp | grep -E '5020|5030'

# 测试 API（健康检查）
curl -fsS http://127.0.0.1:5030/api/login -X POST \
    -H "Content-Type: application/json" \
    -d '{"username":"test","password":"test"}' -w "\nstatus:%{http_code}\n"
```

### 数据库迁移

服务器无 `mysql` client，迁移时使用 `.venv` 里的 `pymysql`：

```bash
cd /opt/ownership_system/server
.venv/bin/python3 <<'PYEOF'
import pymysql
c = pymysql.connect(host="...", port=..., user="...", password="...",
                    database="ownership_system", autocommit=True)
cur = c.cursor()
cur.execute("ALTER TABLE ...")   # 你的迁移 SQL
c.close()
PYEOF
```

新部署参考 `server/migrations/init.sql` 的表结构。

### 与服务器其他项目的隔离

| 项 | 隔离方式 |
|---|---|
| Python 环境 | 独立 `.venv` 在项目目录内，不影响系统 Python |
| 端口 | 5020/5030 与现有项目（5002/5009/5010/81/5181/3000）零冲突 |
| 数据库 | 独立 `ownership_system` 库（公网 RDS 上逻辑隔离） |
| nginx 配置 | 独立 `/etc/nginx/sites-enabled/ownership.conf`，不动其他 server 块 |
| 文件存储 | `uploads/` 在项目目录内，不污染 `/var/www` |
| systemd unit | 独立 `ownership.service`，不影响其他项目 |

---

## 常见问题

**Q: 启动后端报数据库连接错误？**
A: 检查 `.env` 中的 `DB_PASSWORD` 是否正确，数据库是否已创建（`ownership_system`）。

**Q: 前端访问文件提示 404？**
A: 文件通过 `/api/files/{目录}/{文件名}` 访问，需要登录态。确认 `uploads/` 目录存在且后端有读写权限。

**Q: 上传文件失败，报压缩错误？**
A: PDF 压缩依赖 Ghostscript，确认已安装：`gs --version`。图片压缩依赖 Pillow，已在 `requirements.txt` 中。

**Q: 不填写别名能保存作品吗？**
A: 可以。`works.alias` 是可空字段（schema 允许 NULL），前端不填写时后端存空串，不报错。

**Q: 生产环境上传文件提示扩展名被自动更正？**
A: 后端会对上传文件做 MIME 嗅探（PNG/JPEG/GIF/BMP/PDF），如果声明的扩展名与实际二进制签名不符（如把 JPEG 改名为 `.png` 上传），会自动更正后缀并通过顶部黄色提示告知（约 3 秒消失），不影响保存。
